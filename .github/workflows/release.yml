name: Release

# Auto-wired. Every push to main is evaluated for a release; the commit history
# decides. No manual trigger, no version chosen by hand.
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Preview only"
        type: boolean
        default: true

concurrency:
  group: release
  cancel-in-progress: false # never interrupt a half-written release

permissions:
  contents: write
  packages: write # publish to this repo's npm registry
  id-token: write # provenance

jobs:
  release:
    runs-on: ubuntu-latest
    # The release commit tags itself [skip ci]; this is belt-and-braces.
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # full history — versions are derived from it
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"

      - run: bun install --frozen-lockfile

      - name: derive
        id: derive
        run: |
          bun scripts/derive-version.ts
          echo 'plan<<PLAN' >> "$GITHUB_OUTPUT"
          bun scripts/derive-version.ts --json >> "$GITHUB_OUTPUT"
          echo 'PLAN' >> "$GITHUB_OUTPUT"
          COUNT=$(bun scripts/derive-version.ts --json | node -e \
            'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>console.log(JSON.parse(s).filter(p=>p.next).length))')
          echo "count=$COUNT" >> "$GITHUB_OUTPUT"

      - name: dry run
        if: steps.derive.outputs.count != '0' && inputs.dry_run
        run: bun scripts/auto-release.ts --dry-run

      - name: configure git
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: release
        id: release
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        run: bun scripts/auto-release.ts

      - name: push
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        run: git push origin main --follow-tags

      # npmjs is the primary channel: it is the only one of the two that serves
      # anonymous reads. GitHub Packages npm returns 401 to unauthenticated
      # clients even for public packages, so publishing there alone would force
      # every consumer to configure a token just to install.
      - name: publish to npmjs
        id: npmjs
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "::notice::NPM_TOKEN not set — skipping npmjs. Packages remain installable from GitHub Packages (token required)."
            echo "published=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          {
            echo "registry=https://registry.npmjs.org/"
            echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}"
          } > ~/.npmrc

          for TAG in $(git tag --points-at HEAD); do
            DIR="packages/${TAG%@*}"; DIR="${DIR/packages\/opencode-omp-/packages\/}"
            [ -d "$DIR" ] || continue
            echo "npmjs: publishing $TAG from $DIR"
            (cd "$DIR" && npm publish --access public --provenance --registry https://registry.npmjs.org/) \
              || echo "::warning::npmjs publish failed for $TAG (already published?)"
          done
          echo "published=true" >> "$GITHUB_OUTPUT"

      # Mirror. Uses GITHUB_TOKEN, so it always works with no secret to manage.
      - name: mirror to github packages
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          {
            echo "@mrmm:registry=https://npm.pkg.github.com"
            echo "//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}"
          } > ~/.npmrc

          for TAG in $(git tag --points-at HEAD); do
            DIR="packages/${TAG%@*}"; DIR="${DIR/packages\/opencode-omp-/packages\/}"
            [ -d "$DIR" ] || { echo "no package dir for $TAG"; continue; }
            echo "gh-packages: publishing $TAG from $DIR"
            (cd "$DIR" && npm publish --access public --registry https://npm.pkg.github.com) \
              || echo "::warning::gh-packages publish failed for $TAG (already published?)"
          done

      - name: github releases
        if: steps.derive.outputs.count != '0' && !inputs.dry_run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for TAG in $(git tag --points-at HEAD); do
            BARE="${TAG%@*}"
            VER="${TAG##*@}"
            DIR="packages/${BARE#opencode-omp-}"
            NOTES=$(node -e "
              const fs=require('fs');
              const cl=fs.readFileSync('$DIR/CHANGELOG.md','utf8');
              const esc='$VER'.replace(/\./g,'\\\\.');
              const m=cl.match(new RegExp('^## \\\\['+esc+'\\\\][^\\\\n]*\\\\n([\\\\s\\\\S]*?)(?=^## \\\\[|\$)','m'));
              const body=(m?m[1]:'').trim();
              console.log(body ? body+'\n\n---\n\nInstall: \`bun add @mrmm/$BARE\`' : 'See CHANGELOG.');
            ")
            gh release create "$TAG" --title "$BARE $VER" --notes "$NOTES" || true
          done

      - name: summary
        if: always()
        run: |
          {
            echo "### Release"
            echo ''
            if [ "${{ steps.derive.outputs.count }}" = "0" ]; then
              echo 'Nothing to release — no release-worthy commits since the last tags.'
            else
              echo '```'
              bun scripts/derive-version.ts || true
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"
